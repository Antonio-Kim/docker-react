version: 2.1
jobs:
  build:
    docker:
      - image: ubuntu-2004:202107-02
    steps:
      - checkout
      - run:
          name: Pre-build image
          command: docker build -t antoniok/docker-react -f Dockerfile.dev .
      - run:
          name: script
          command: docker run -it antoniok/docker-react yarn test -- --coverage

workflows:
  running_test:
    jobs:
      - build 